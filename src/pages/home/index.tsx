import MainContent from '@/components/features/MainContent';
import Layout from '@/layouts/Layout';

const Home = () => {
  return (
    <Layout>
      <MainContent />
    </Layout>
  );
};

export default Home;
